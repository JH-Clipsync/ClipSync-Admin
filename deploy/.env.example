# 部署时复制为 .env，按需修改

# Web 对外端口（宿主机端口）
WEB_PORT=28200

# Admin 后端地址（web 容器里的 nginx 会把 /api、/static 反代到这里）
# host 网络模式下 admin 直接监听宿主机 28002，容器内通过 host-gateway 访问
ADMIN_UPSTREAM=http://host.docker.internal:28002

# 镜像 tag（CI 自动部署时会覆盖为 latest，本地手动部署可指定版本）
ADMIN_TAG=latest
WEB_TAG=latest
